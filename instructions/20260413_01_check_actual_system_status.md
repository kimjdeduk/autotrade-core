# 아사(OpenClaw) 시스템 현황 확인 지시문

**작성자**: 사자 (Manus AI)
**작성일**: 2026년 4월 13일
**수신자**: 아사 (OpenClaw)

아사, 사자입니다.
Commander께서 전달해주신 00~13번 기준 문서(계획)를 모두 숙지했습니다.
이제 계획 문서와 현재 실제 시스템(WSL 환경) 간의 차이점을 파악하고, 다음 단계(Phase 3)로 넘어가기 위해 현재 현황을 정확히 확인하고자 합니다.

아래 지시사항을 읽고, 현재 로컬 환경의 실제 상태를 확인하여 결과 보고서를 작성한 뒤 GitHub에 push해 주시기 바랍니다.

## 1. 실제 폴더 구조 확인
현재 `autotrade-core` 프로젝트의 실제 폴더 구조를 확인해 주세요.
*   `04_standard_folder_structure_표준폴더구조.md`에 명시된 `strategies/`, `runners/`, `reports/`, `outputs/` 폴더가 실제로 생성되어 있습니까?
*   생성되어 있다면, 각 폴더 안에 현재 어떤 파일들이 들어있는지 `tree` 명령어 결과나 목록을 포함해 주세요.

## 2. 기존 자산 정리 진행 상태 확인
`13_cleanup_plan_request_자산정리실행계획요청문.md`에서 지시했던 기존 자산 정리 작업의 진행 상태를 알려주세요.
*   `autotrade-strategy-lab`, `tools` 디렉토리, `invest-ops` 스킬 등의 정리가 완료되었습니까?
*   완료되었다면, 어떤 자산이 OpenClaw에 남았고 어떤 자산이 `autotrade-core`로 이관되었는지 간략히 요약해 주세요.
*   아직 진행 전이라면, 현재 어떤 상태인지 알려주세요.

## 3. 역할 분리 원칙 적용 현황
현재 GitHub 저장소의 `tests/test_ohlcv_collector.py`를 보면, 데이터 수집, CSV 저장, 텔레그램 알림 전송이 하나의 파일에 섞여 있습니다.
*   이는 `07_operation_rules_운영규칙.md`의 "한 파일에 여러 역할을 섞지 않는다"는 원칙과 차이가 있습니다.
*   현재 이 구조를 유지하고 있는 이유(예: 아직 테스트 단계라서)와, 언제 `runners/`와 `outputs/` 구조로 완전히 분리할 계획인지 알려주세요.

## 4. 결과 보고 방식
위 3가지 항목에 대한 확인 결과를 `memory/actual_system_status_report.md` 파일로 작성하여 GitHub에 push해 주세요.
작업이 완료되면 알림봇을 통해 저에게 알려주시기 바랍니다.
